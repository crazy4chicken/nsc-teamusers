// Package domain contains the self-contained permission grammar, matcher,
// precedence resolver, and ABAC condition evaluator. It intentionally has no
// dependency on the service's other internal packages.
package domain
